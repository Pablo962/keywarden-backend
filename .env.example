# ============================================
# KEYWARDEN - Configuración de Entorno
# ============================================
# Copiar este archivo a .env y configurar con valores reales

# --- Configuración del Servidor ---
PORT=4000

# --- Configuración de la Base de Datos ---
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_NAME=admtfi

# --- Configuración de Seguridad (JWT) ---
# IMPORTANTE: En producción, usar una frase larga y aleatoria
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=UNA_FRASE_SUPER_SECRETA_Y_LARGA_PARA_MIS_TOKENS

# --- Configuración de Entorno ---
NODE_ENV=development

# --- Configuración de CORS (Opcional) ---
# CORS_ORIGIN=http://localhost:3000

# --- Notas de Seguridad ---
# 1. NUNCA subir el archivo .env al repositorio
# 2. Asegurar que .env está en .gitignore
# 3. Cambiar JWT_SECRET en producción
# 4. Usar contraseñas fuertes para la base de datos
